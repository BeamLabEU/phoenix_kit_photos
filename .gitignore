/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
.lexical/

# assets/ toolchain
/assets/node_modules/

# NOTE: priv/static/assets/*.js is the prebuilt bundle js_sources/0 points at.
# It ships in the Hex package, so it is deliberately NOT ignored.
